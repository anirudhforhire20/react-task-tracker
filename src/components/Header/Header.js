import './Header.css';
const Header = () => {
    return (
        <header className="Header">
            <h1>Task tracker</h1>
            <p>A react task tracker app</p>
        </header>
    );
}

export default Header;